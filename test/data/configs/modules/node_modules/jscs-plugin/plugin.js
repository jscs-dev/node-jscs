module.exports = function(conf) {
    conf.registerRule({
        getOptionName: function() {
            return 'test';
        },
        configure: function(config) {
            this.config = config;
        }
    });
}
